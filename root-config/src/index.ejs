<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>MFE Architecture — single-spa + Module Federation</title>
    <style>
      * { box-sizing: border-box; margin: 0; padding: 0; }
      body { font-family: system-ui, sans-serif; background: #0f172a; color: #e2e8f0; min-height: 100vh; }
    </style>
  </head>
  <body>
    <!--
      single-spa-layout injects the DOM structure from root-config.js `routes`.
      Do NOT hardcode application containers here — the layout engine creates them.
    -->
    <script src="/root-config.js"></script>
  </body>
</html>
